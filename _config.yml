title: ActiveCanvas
description: The AI-powered CMS engine for Rails 8+. Visual page builder, AI content generation, and production-ready Tailwind CSS — all in one mountable gem.
url: "https://activecanvas.dev"
baseurl: ""

collections:
  guides:
    output: true
    permalink: /guides/:slug.html

defaults:
  - scope:
      path: ""
      type: "guides"
    values:
      layout: docs

exclude:
  - node_modules
  - package.json
  - package-lock.json
  - Procfile.dev
  - _src
  - Gemfile
  - Gemfile.lock
  - README.md
